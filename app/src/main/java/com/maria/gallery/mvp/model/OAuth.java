package com.maria.gallery.mvp.model;

public class OAuth {
    public static String auth = "Authorization";
    public static String oauth = "OAuth " + OAuth.token;

    public static String token = "";
}
